import variable from "../sass/variables.scss";

const colors = {
  primary: variable.primary,
  secondary: variable.secondary,
  background: variable.background,
  secondary_background: variable.secondary_background,
  highlight: variable.highlight,
  text_color: variable.text_color,
};

export default colors;
